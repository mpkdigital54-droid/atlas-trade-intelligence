{
  "name": "atlas-trade-intelligence",
  "version": "1.0.0",
  "description": "Atlas Trade Intelligence — Global Trade & Financial Clearance Platform",
  "private": true,
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build needed'",
    "start": "npx serve public -p 3000"
  }
}
